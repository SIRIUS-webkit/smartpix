import { Inter, Fugaz_One } from "next/font/google";

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const fugaze_one = Fugaz_One({
  subsets: ["latin"],
  weight: ["400"],
  variable: "--font-fugaze_one",
});
